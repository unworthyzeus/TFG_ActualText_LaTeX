\chapter{Methodology / project development}

This chapter describes the dataset protocol, the evolution of the modelling
approach, and the final Try 80 architecture. The emphasis is on decisions that
affect reproducibility: masking, splitting, conditioning, and the distinction
between raw deep prediction and prior-anchored residual prediction.

\section{Dataset protocol}

Each sample is a complete $513\times513$ pixel CKM. The model never receives a
random crop as an independent training city. Instead, the split is performed at
city level. For the final Try 76, Try 77, and Try 80 family the split uses
\texttt{split\_seed = 42}, with 15\% validation and 15\% test samples after
city assignment. This avoids the most common optimistic error in spatial radio
prediction: training on one part of a city and testing on another visually
similar part of the same city.

The receiver mask is:
\begin{equation}
  m_{\mathrm{ground}}(i,j) =
  \begin{cases}
    1, & \text{if } \mathrm{topology}(i,j)=0,\\
    0, & \text{otherwise.}
  \end{cases}
  \label{eq:ground_mask}
\end{equation}
All RMSE values in this thesis are pixel-weighted over valid ground pixels:
\begin{equation}
  \mathrm{RMSE}
  =
  \sqrt{
  \frac{\sum_{s,i,j} m_s(i,j)\left(\hat y_s(i,j)-y_s(i,j)\right)^2}
       {\sum_{s,i,j} m_s(i,j)}
  }.
  \label{eq:rmse}
\end{equation}
This is stricter than averaging per-sample RMSEs because large valid maps
contribute proportionally to the number of receiver pixels.

\section{From U-Net and cGAN to masked baselines}

The first implementation used a U-Net generator with a PatchGAN discriminator.
The generator predicted channel maps from topology-related inputs, while the
discriminator encouraged realistic local patches. This was useful as a
software prototype but not as a final scientific direction. The adversarial
term did not improve the physical errors that dominated validation RMSE, and it
made diagnosis harder.

The next phase focused on controlled engineering changes. Transposed
convolutions were replaced by bilinear upsampling and projection to reduce
checkerboard artifacts. Multi-scale supervision was added so that the model
could learn both global radial structure and local map details. Try 22 became
the strongest clean pre-prior path-loss baseline in this family.

After Try 33, building pixels were excluded from both training loss and
validation metrics. This changed the interpretation of earlier numbers: any
method that implicitly benefited from easy building pixels was no longer
acceptable. The ground-only rule became an invariant for later tries.

\section{Chronological attempt analysis}

The work was not a single clean jump from U-Net to the final Try 80 model. It
was closer to an engineering search in which each group of attempts tested one
hypothesis and either kept it, rejected it, or transformed it into a stricter
version. Table~\ref{tab:attempt_evolution} summarizes the most important
steps. Values are included only when the metric contract is sufficiently clear;
otherwise the row records the technical lesson rather than forcing an
unreliable comparison.

\begin{table}[ht]
\centering
\caption{Evolution of modelling techniques and lessons across the attempt
history.}
\label{tab:attempt_evolution}
\scriptsize
\begin{tabularx}{\textwidth}{p{1.8cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X}
\toprule
Attempts & Main technique tested & What worked & What did not work and why \\
\midrule
1--8 &
U-Net and pix2pix-style cGAN for aligned image-to-image regression. &
Established the full CKM pipeline and showed that dense convolutional
prediction was feasible at map level. &
Adversarial realism was not aligned with RMSE. It could create plausible local
texture while leaving the physical propagation error unresolved. \\
9--14 &
Explicit UAV height, LoS/NLoS splits, non-finite masking, normalized height
conditioning. &
Made height and propagation regime visible to the model instead of hidden in
the sample identity. &
The network still had to rediscover the radial propagation law from pixels,
which was inefficient and unstable across cities. \\
15--19 &
GroupNorm, distance-map channel, weaker or zero GAN pressure, controlled
smoothing. &
GroupNorm was better suited to small batches; explicit distance helped the
large-scale attenuation trend. &
GAN pressure mostly attacked appearance artifacts, not the hard physical
errors. Too much smoothing erased real transitions. \\
20--22 &
Bilinear decoder and multiscale path-loss loss. &
Try 22 became the strongest clean pre-prior path-loss baseline, with observed
validation RMSE around \SI{16.72}{dB} in that early contract. &
The model was cleaner but still missed global radial structure and NLoS tails. \\
23--32 &
Spread-branch revisits, radial losses, support-amplitude decomposition, first
prior-plus-residual ideas. &
The experiments identified two separate problems: path loss needed radial
physics, while spreads needed support and amplitude protection. &
Attention or extra capacity alone did not solve the propagation structure.
The first residual priors were conceptually promising but not yet strong. \\
33--40 &
Building-mask exclusion and restart on the newer dataset. &
Established the modern evaluation rule: building pixels are invalid receiver
locations and must be excluded from loss, metrics, and error maps. &
Older scores became non-comparable. Some apparent progress vanished under the
stricter ground-only contract. \\
41--50 &
Physical prior plus learned residual, PMNet-style backbone, prior confidence,
and NLoS-tail refiner ideas. &
Train-only calibration reduced a raw prior from about \SI{67.23}{dB} to about
\SI{24.16}{dB}; Try 49 stage 1 reached about \SI{18.87}{dB} against a
\SI{23.57}{dB} prior. &
NLoS remained heavy-tailed. Several richer NLoS priors did not beat the older
calibration; the best Try 50 NLoS sandbox remained around \SI{41}{dB}. \\
51--66 &
PMHHNet consolidation with experts, high-frequency path, physical channels,
and strict city-holdout reporting. &
Try 66 reached \SI{9.41}{dB} overall, with LoS already near
\SI{3.75}{dB}. &
The remaining error was not uniform; it concentrated in NLoS regions
(\SI{31.48}{dB}), especially low-altitude and obstructed cases. \\
67--75 &
Anti-overfitting, multi-expert variants, dual heads, heteroscedastic outputs,
SWA/EMA-style training refinements. &
These runs improved training discipline and made uncertainty and regime
reporting more explicit. &
Some configurations over-regularized the model or anchored predictions too
close to the prior, so validation stalled despite more sophisticated losses. \\
76--77 &
Distribution-first GMM modelling for path loss and then spreads. &
Try 76 showed that NLoS can be treated as a distributional problem; the
DirectML NLoS summary reached about \SI{3.34}{dB} pixel-weighted RMSE. &
The method was powerful but task-specific; Try 77 provided a spread design,
while final spread claims later came from the calibrated Try 79 prior. \\
78--80 &
Frozen non-DL priors for path loss and spreads, then a joint prior-anchored
residual model. &
Try 78 reached \SI{1.9246}{dB} overall path-loss RMSE; Try 79 reached
\SI{28.10}{ns} and \SI{13.74}{\degree}; Try 80 unifies all three targets. &
Try 80 still requires a frozen final checkpoint before its test result can be
reported as the final thesis claim. \\
\bottomrule
\end{tabularx}
\end{table}
\normalsize
This history changed the interpretation of the project. The final model is not
only the largest or latest network. It is the result of four negative lessons:
GANs were not reliable RMSE optimizers for this physical target; building
pixels had to be removed from the metric; pure CNNs underlearned radial LoS
structure; and NLoS errors were heavy-tailed enough to require regime-aware
or distribution-aware treatment. These lessons justify the final
prior-anchored formulation.

\section{Physical priors and PMHHNet}

Attempts 41 to 75 moved from direct map prediction to residual learning, as
formulated in Eq.~\ref{eq:prior_residual}. This phase was heavily influenced by
the \texttt{PMNet} architecture \cite{pmnet2023}, which established the
viability of using U-Net-like backbones for path-loss map prediction in
terrestrial cellular networks.
\begin{equation}
  \hat{\textbf{Y}}
  =
  \textbf{P}_{\mathrm{phys}}
  +
  f_{\theta}\!\left(\mathcal{T}, h, \textbf{P}_{\mathrm{phys}}\right),
  \label{eq:prior_residual}
\end{equation}
where \(\textbf{P}_{\mathrm{phys}}\) is the analytical or calibrated prior map
and \(f_{\theta}\) predicts only the residual correction that the prior cannot
explain.

\subsection{Mathematical Evolution: From PMNet to PMHHNet}

The \texttt{PMHHNet} (Path-loss Map with High-frequency and Height-awareness
Network) was developed as a specific refinement of the \texttt{PMNet}
philosophy tailored for the UAV-enabled Air-to-Ground (A2G) scenario.

\paragraph{High-Frequency (HF) Stem.}
Standard convolutional encoders tend to smooth out sharp building transitions
during downsampling. To counter this, \texttt{PMHHNet} introduces a dedicated
high-frequency stem. Let $\mathcal{T}$ be the topology map. We compute the
discrete Laplacian $|\nabla^2 \mathcal{T}|$ to identify building edges and
shadow boundaries. The initial feature map $\textbf{X}_0$ is computed as:
\begin{equation}
  \textbf{X}_0 = \mathrm{Stem}(\mathcal{T}) + \mathrm{Proj}(|\nabla^2 \mathcal{T}|)
  \label{eq:hf_stem}
\end{equation}
where $\mathrm{Proj}$ is a $1\times1$ convolution that maps the edge-detector
output to the stem's feature depth. This ensures that building geometry is
strongly injected into the earliest layers of the encoder.

\paragraph{Continuous Sinusoidal FiLM Conditioning.}
While \texttt{PMNet} was designed for fixed base-station heights, UAVs require
continuous height generalization. \texttt{PMHHNet} utilizes Feature-wise Linear
Modulation (FiLM) \cite{perez2018film} to inject the scalar height $h$.
The height is first mapped to a sinusoidal embedding $\textbf{e}(h) \in \mathbb{R}^K$
(Eq.~\ref{eq:sin_film}). For any feature map $\textbf{X} \in \mathbb{R}^{C \times H \times W}$
at depth $l$, the conditioned map $\textbf{X}'$ is:
\begin{equation}
  \textbf{e}(h)
  =
  \left[
    \sin(\omega_1 \tilde{h}), \cos(\omega_1 \tilde{h}), \ldots,
    \sin(\omega_{K/2} \tilde{h}), \cos(\omega_{K/2} \tilde{h})
  \right],
  \qquad
  \tilde{h} = \frac{h-h_{\min}}{h_{\max}-h_{\min}},
  \label{eq:sin_film}
\end{equation}
\begin{equation}
  \textbf{X}' = \textbf{X} \odot (1 + \textbf{g}_l(\textbf{e}(h))) + \textbf{b}_l(\textbf{e}(h))
  \label{eq:film_math}
\end{equation}
where $\textbf{g}_l$ and $\textbf{b}_l$ are learned linear projections mapping the
embedding to the channel dimension $C$. This pattern, inspired by time-step
conditioning in diffusion models \cite{dhariwal2021diffusion}, allows the
network to modulate its internal representation dynamically based on the UAV's
altitude.

\subsection{Limitations and Legacy}

Despite these improvements, the early residual phase plateaued at about
\SI{9.4}{dB} (Try 66). The primary bottleneck was the "soft" nature of the
residual learning; the network often under-compensated for the physical
propagation law in NLoS regions.

\begin{quote}
\textit{Legacy of PMHHNet:} While the full architecture was eventually superseded
by the prior-anchored GMM approach of \texttt{Try 80}, its core components---the
HF stem and sinusoidal FiLM---remain highly effective for point-prediction
tasks or real-time implementations where the distributional complexity of a GMM
is not required.
\end{quote}

\section{Distribution-first modelling}
\label{sec:dist_first}

The decisive diagnosis after Try 75 was that NLoS path loss was not merely a
harder regression target; it possessed a fundamentally different error
distribution. Try 76 addressed this by predicting the parameters of a Gaussian
Mixture Model (GMM) rather than a simple point estimate.

\begin{figure}[p]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    >=stealth, thick,
    nd/.style={draw, rectangle, rounded corners=3pt, minimum width=2.6cm,
               minimum height=0.65cm, align=center, font=\scriptsize},
    inp/.style={nd, fill=blue!10},
    sa/.style={nd, fill=red!10},
    sb/.style={nd, fill=green!10},
    ob/.style={nd, fill=orange!10},
    grp/.style={draw=orange!50, fill=yellow!8, rounded corners=4pt, inner sep=8pt},
    lbl/.style={font=\tiny\itshape, anchor=south west, yshift=2pt},
]

%% Inputs row
\node[inp] (topo)  {topology\_map / 90};
\node[inp, right=0.4cm of topo]  (masks) {los, nlos,\\ground masks};
\node[inp, right=0.4cm of masks] (hgt)   {UAV height\\embedding};
\node[nd,  right=1.4cm of hgt]   (filt)  {Invalid\\target filter};

%% Routing row
\node[nd, fill=blue!15, below=0.5cm of topo]             (cls) {6 topology classes};
\node[nd, fill=blue!20, minimum width=3.2cm,
      below=0.5cm of masks, xshift=0.6cm]                (exp) {12 experts = 6 classes $\times$\\LoS / NLoS};
\node[nd, fill=blue!15, below=0.5cm of filt]             (vld) {valid\_target\\finite and $\ge 0$};

\draw[->] (topo) -- (cls);
\draw[->] (cls)  -- (exp);
\draw[->] (masks)-- (exp);
\draw[->] (hgt)  -- (exp);
\draw[->] (filt) -- (vld);

\begin{pgfonlayer}{background}
  \node[grp, fit=(topo)(masks)(hgt)(filt)(cls)(exp)(vld)] (inp_box) {};
\end{pgfonlayer}
\node[lbl] at (inp_box.north west) {Inputs and routing};

%% Stage A — GMM head (right)
\node[sa, below=1.2cm of exp]       (enc)  {Encoder with FiLM(height)};
\node[sa, below=0.4cm of enc]       (gap)  {Global average pooling};
\node[sa, below=0.4cm of gap]       (gmm)  {GMM head};
\node[sa, below=0.4cm of gmm]       (outa) {$\pi_k,\,\mu_k,\,\sigma_k$};

\draw[->] (exp) -- (enc);
\draw[->] (enc) -- (gap);
\draw[->] (gap) -- (gmm);
\draw[->] (gmm) -- (outa);

\begin{pgfonlayer}{background}
  \node[grp, fit=(enc)(gap)(gmm)(outa)] (sa_box) {};
\end{pgfonlayer}
\node[lbl] at (sa_box.north west) {Stage A --- distribution head};

%% Stage B — map head (left of Stage A)
\node[sb, left=1.6cm of enc, yshift=-0.4cm]  (dec)  {U-Net decoder};
\node[sb, below=0.4cm of dec]                 (cond) {Condition on height +\\GMM summary};
\node[sb, below=0.4cm of cond]                (pix)  {Per-pixel $p_k,\,z_k,\,\log\sigma_k$};
\node[sb, below=0.4cm of pix]                 (yhat) {$\hat{y}=\sum p_k(\mu_k+z_k\sigma_k)$};

\draw[->] (dec)  -- (cond);
\draw[->] (cond) -- (pix);
\draw[->] (pix)  -- (yhat);
\draw[->] (outa.west) -- ++(-0.25,0) |- (cond.east);

\begin{pgfonlayer}{background}
  \node[grp, fit=(dec)(cond)(pix)(yhat)] (sb_box) {};
\end{pgfonlayer}
\node[lbl] at (sb_box.north west) {Stage B --- map head};

%% Objective row
\node[ob, below=1.2cm of yhat]     (o1) {map\_nll under\\mixture};
\node[ob, right=0.3cm of o1]       (o2) {moment\_match};
\node[ob, right=0.3cm of o2]       (o3) {rmse\_db};
\node[ob, right=0.3cm of o3]       (o4) {outlier\_budget};
\node[ob, right=1.0cm of o4]       (o5) {dist\_kl on\\empirical hist.};

\draw[->] (yhat.south) -- ++(0,-0.4) -| (o1.north);
\draw[->] (yhat.south) -- ++(0,-0.4) -| (o2.north);
\draw[->] (yhat.south) -- ++(0,-0.4) -| (o3.north);
\draw[->] (yhat.south) -- ++(0,-0.4) -| (o4.north);
\draw[->] (outa.south) -- ++(0,-0.4) -| (o5.north);

\begin{pgfonlayer}{background}
  \node[grp, fit=(o1)(o2)(o3)(o4)(o5)] (ob_box) {};
\end{pgfonlayer}
\node[lbl] at (ob_box.north west) {Training objective on valid/bound pixels};

\end{tikzpicture}%
}
\caption{Try 76 distribution-first path-loss system. Stage~A learns global
GMM parameters $(\pi_k,\mu_k,\sigma_k)$ from pooled features conditioned on
UAV height; Stage~B produces per-pixel soft assignments and
within-component offsets anchored to those parameters.}
\label{fig:try76_system}
\end{figure}

Try 77 applied the same idea to delay spread and angular spread. These targets
are closer to a "spike-plus-tail" distribution (a spike near zero for LoS, and
a heavy tail for NLoS).

\begin{figure}[p]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    >=stealth, thick,
    nd/.style={draw, rectangle, rounded corners=3pt, minimum width=2.6cm,
               minimum height=0.65cm, align=center, font=\scriptsize},
    inp/.style={nd, fill=blue!10},
    sa/.style={nd, fill=red!10},
    sb/.style={nd, fill=green!10},
    ob/.style={nd, fill=orange!10},
    grp/.style={draw=orange!50, fill=yellow!8, rounded corners=4pt, inner sep=8pt},
    lbl/.style={font=\tiny\itshape, anchor=south west, yshift=2pt},
]

%% Inputs row
\node[inp] (topo)  {topology\_map / 90};
\node[inp, right=0.4cm of topo]  (masks) {los, nlos,\\ground masks};
\node[inp, right=0.4cm of masks] (hgt)   {UAV height\\embedding};
\node[nd,  right=1.4cm of hgt]   (filt)  {Invalid\\target filter};

%% Routing row
\node[nd, fill=blue!15, below=0.5cm of topo]             (cls) {6 topology classes};
\node[nd, fill=blue!20, minimum width=3.4cm,
      below=0.5cm of masks, xshift=0.6cm]                (exp) {12 experts = 6 classes $\times$\\(delay\_spread, angular\_spread)};
\node[nd, fill=blue!15, below=0.5cm of filt]             (vld) {valid\_target\\finite and $\ge 0$};

\draw[->] (topo) -- (cls);
\draw[->] (cls)  -- (exp);
\draw[->] (masks)-- (exp);
\draw[->] (hgt)  -- (exp);
\draw[->] (filt) -- (vld);

\begin{pgfonlayer}{background}
  \node[grp, fit=(topo)(masks)(hgt)(filt)(cls)(exp)(vld)] (inp_box) {};
\end{pgfonlayer}
\node[lbl] at (inp_box.north west) {Inputs and routing};

%% Stage A — spike+tail head (right)
\node[sa, below=1.2cm of exp]              (enc)  {Encoder with FiLM(height)};
\node[sa, below=0.4cm of enc]              (pool) {Global pooled context};
\node[sa, below=0.4cm of pool]             (head) {Spike + tail mixture head};
\node[sa, below=0.5cm of head, xshift=-1.4cm] (ck) {Components 1..K:\\heavy-tail mixture};
\node[sa, below=0.5cm of head, xshift=+1.4cm] (c0) {Component 0:\\near-zero spike};

\draw[->] (exp)  -- (enc);
\draw[->] (enc)  -- (pool);
\draw[->] (pool) -- (head);
\draw[->] (head) -- (ck);
\draw[->] (head) -- (c0);

\begin{pgfonlayer}{background}
  \node[grp, fit=(enc)(pool)(head)(ck)(c0)] (sa_box) {};
\end{pgfonlayer}
\node[lbl] at (sa_box.north west) {Stage A for spreads};

%% Stage B — decoder (left)
\node[sb, left=1.6cm of enc, yshift=-0.4cm]  (dec)  {U-Net decoder};
\node[sb, below=0.4cm of dec]                 (cond) {Condition on height +\\mixture summary};
\node[sb, below=0.4cm of cond]                (pix)  {Per-pixel $p_k(j),\,z(j),\,\log\sigma_k(j)$};
\node[sb, below=0.4cm of pix]                 (yhat) {$\hat{y}(j)=\sum p_k(\mu_k+z\sigma_k)$};

\draw[->] (dec)  -- (cond);
\draw[->] (cond) -- (pix);
\draw[->] (pix)  -- (yhat);
\draw[->] (pool.west) -- ++(-0.25,0) |- (cond.east);
\draw[->] (ck.west)   -- ++(-0.3, 0) |- (yhat.east);

\begin{pgfonlayer}{background}
  \node[grp, fit=(dec)(cond)(pix)(yhat)] (sb_box) {};
\end{pgfonlayer}
\node[lbl] at (sb_box.north west) {Stage B decoder};

%% Objective row
\node[ob, below=1.2cm of yhat]     (o1) {outlier\_budget};
\node[ob, right=0.3cm of o1]       (o2) {rmse and mae};
\node[ob, right=0.3cm of o2]       (o3) {moment\_match};
\node[ob, right=0.3cm of o3]       (o4) {dist\_kl};
\node[ob, right=0.3cm of o4]       (o5) {map\_nll};
\node[nd, fill=yellow!30, right=0.6cm of o5] (lm) {loss\_mask =\\ground\_mask $\times$ valid};

\draw[->] (yhat.south) -- ++(0,-0.4) -| (o1.north);
\draw[->] (yhat.south) -- ++(0,-0.4) -| (o2.north);
\draw[->] (yhat.south) -- ++(0,-0.4) -| (o3.north);
\draw[->] (yhat.south) -- ++(0,-0.4) -| (o4.north);
\draw[->] (yhat.south) -- ++(0,-0.4) -| (o5.north);
\draw[->] (vld.south)  -- ++(0,-0.6)  -| (lm.north);

\begin{pgfonlayer}{background}
  \node[grp, fit=(o1)(o2)(o3)(o4)(o5)(lm)] (ob_box) {};
\end{pgfonlayer}
\node[lbl] at (ob_box.north west) {Objective};

\end{tikzpicture}%
}
\caption{Try 77 spread-prediction system. The Stage~A mixture head
separately models the near-zero spike (LoS direct path) and the
heavy-tailed components (NLoS scattering). Stage~B decodes these into
spatially varying per-pixel predictions.}
\label{fig:try77_system}
\end{figure}



For path loss, Try 76 utilized 12 specialized experts: six topology classes partitioned by LoS
and NLoS regions. As shown in Figure~\ref{fig:try76_system}, Stage A acts as a distribution head, predicting the global GMM parameters
$(\pi_k,\mu_k,\sigma_k)$ from map features and altitude conditioning.
Stage B then decodes these priors spatially, predicting per-pixel soft component assignments and within-component
offsets. The final map reconstruction can be written as:
\begin{equation}
  \hat y(i,j)
  =
  \sum_{k=1}^{K} p_k(i,j)
  \left(\mu_k + z_k(i,j)\sigma_k\right).
  \label{eq:gmm_reconstruction}
\end{equation}

Try 77 extended this distribution-first philosophy to delay spread and angular spread. As illustrated in Figure~\ref{fig:try77_system}, these targets
are not cleanly Gaussian; their empirical distributions resemble a near-zero spike (representing the LoS direct path) combined with a
heavy tail (representing NLoS scattering). Standard Mean Squared Error (MSE) objectives tend to underrepresent these rare
but physically significant heavy-tail spread values, necessitating a dedicated mixture head.



\section{Try 78 and Try 79 priors: overview and literature basis}
\label{sec:priors_overview}

\input{prior_detail_overview}

\section{Try 78: Path Loss Prior}
\label{sec:try78_detail}

\input{prior_detail_try78}

\section{Try 79: Delay and Angular Spread Priors}
\label{sec:try79_detail}

\input{prior_detail_try79}

\section{Try 80: Joint Prior-Anchored Residual Model}
\label{sec:try80_detail}

\input{prior_detail_try80}

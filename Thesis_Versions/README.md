# Thesis version snapshots

This folder keeps two thesis versions inside the same repository:

- `previous_git_e86fa44`: snapshot of commit `e86fa44`, the previous git version before the current compacted work. The compiled PDF has 157 pages.
- `compacted_current`: current compacted thesis snapshot after the supervisor-feedback and SoA-comparison edits. The compiled PDF has 148 pages.

Each version contains its own `TFG` source folder and a compiled PDF at the folder root.

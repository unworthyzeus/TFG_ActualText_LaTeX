\clearpage
\chapter{Development Phases and Attempt Trace}
\begingroup
\small

This appendix preserves the development evidence behind the final method while
keeping the main chapters final method first. It records why older attempts are
not presented as competing final systems, which design decisions survived into
\textsc{HARP-Net CKM}, and which negative results are useful mainly as
warnings. The full row level attempt map remains in \texttt{appendices.tex} and
in the repository history; this compiled appendix keeps the evidence that is
needed to understand the final design.

\begin{table}[htbp]
\centering
\caption{Compact attempt log grouped by methodological phase.}
\label{tab:compact_attempt_log}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\textwidth}{p{0.14\textwidth}p{0.26\textwidth}X}
\toprule
Attempt range & Phase & Why it matters in the final thesis \\
\midrule
1--22 & Direct image to image baselines & Established the CKM loading, normalization, aligned map output, cGAN/U-Net baselines, bilinear decoder fixes and multiscale supervision. The phase ended because plausible images did not guarantee physical RMSE. \\
23--40 & Spread branches and mask reset & Added delay/angular experiments and, most importantly, the valid ground receiver mask. This changed the evaluation contract and made earlier scores historical only. \\
41--50 & Prior residual PMNet phase & Showed that raw formulas were weak, train only calibration helped, and residual learning was useful only when the anchor already carried physical structure. \\
51--66 & PMHHNet and topology experts & Introduced topology experts, height FiLM, high frequency paths, obstruction channels and no prior controls. LoS improved, but NLoS remained the dominant error source. \\
67--75 & Stabilisation and distribution diagnosis & Tested knife edge channels, quad heads, uncertainty and histogram diagnostics. The key result was the visible NLoS and spread tail distribution mismatch. \\
76--77 & Distribution first models & GMM and spike plus tail heads showed that modelling target distributions before pixel assignment could remove much of the collapse. \\
78--79 & Frozen calibrated priors & Produced the final train only attenuation and spread priors, including height aware LoS two ray calibration and log domain spread ridge calibration. \\
80 & \textsc{HARP-Net CKM} & Combined frozen priors with a bounded shared residual model and selected the RMSE oriented fine tuning stage reported in Chapter~\ref{chap:results}. \\
\bottomrule
\end{tabularx}
\end{table}

The main methodological lesson is that the project did not progress by simply
making a larger network. The decisive changes were the receiver valid
evaluation contract, regime separation, distribution diagnostics, and the return
to strong frozen priors once their role had become clear.

\section{Expanded phase notes}

The compact table above is intentionally phase based, but the thesis still
needs enough chronology to show that the final model was not chosen in one
step. The following notes keep the development evidence that most directly
explains the final design.

\begin{table}[htbp]
\centering
\caption{Decisive attempt milestones kept from the detailed attempt log.}
\label{tab:expanded_attempt_milestones}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\textwidth}{p{0.11\textwidth}p{0.28\textwidth}X}
\toprule
Attempt & Public role & Lasting lesson \\
\midrule
22 & Clean early image to image baseline & Bilinear decoding and multiscale supervision reduced visual artifacts, but direct image realism was still a weak proxy for calibrated radio map error. \\
33--40 & Ground mask reset & Building pixels had to be excluded from loss, metrics and visual error maps; scores before and after this contract are historical, not directly comparable. \\
41--45 & First explicit prior residual models & Raw formulas were weak, train only calibration helped strongly, and residual learning was useful only when the prior carried real physical structure. \\
50 & NLoS prior sandbox & Scalar NLoS corrections, tabular deltas and simple hybrid formulas did not solve blocked link tails. The remaining error was spatial and distributional. \\
54--66 & PMHHNet and topology experts & Height FiLM, physical channels and six topology experts improved the easy LoS regime, but NLoS stayed dominant even after substantial backbone engineering. \\
67--75 & Stabilisation and histogram diagnosis & Knife edge channels, quad heads and uncertainty improved diagnosis more than final RMSE. Histogram plots made the NLoS and spread tail mismatch explicit. \\
76 & Distribution first PL GMM & Predicting the target distribution before assigning pixels gave the first convincing route for hard attenuation tails. \\
77 & Distribution first spread GMM & Delay and angular spread behaved like spike plus tail targets rather than smooth images, motivating distribution aware output heads. \\
78--79 & Frozen calibrated priors & The strongest non neural evidence came from height aware LoS calibration and log domain spread ridge calibration, both fitted only on training cities. \\
80 & \textsc{HARP-Net CKM} & The selected system is a calibrated prior estimator plus bounded learned residual distribution head, not a black box direct regressor. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{How to read old attempt numbers}

The attempt numbers are chronological engineering labels, not a leaderboard.
They mix changes in data loading, masking, target units, model capacity, loss
design and reporting. A lower RMSE from an early attempt may be invalid under
the final contract if it used building pixels as easy background, evaluated a
different split, or targeted only one output. For that reason, this appendix
separates \emph{evidence} from \emph{final claims}: old attempts explain why a
choice was made, while Chapter~\ref{chap:results} reports only the frozen prior
and \textsc{HARP-Net CKM} metrics on the final unseen city split.

\paragraph{PMNet transfer check.}
A separate diagnostic placed official PMNet checkpoints on CKM samples. This
confirmed that PMNet family models are relevant as architectural references but
cannot be imported as ready made solutions for this thesis contract. The
training and target distributions are structurally different: CKM uses
continuous UAV height, full \(513\times513\) city maps, city holdout
evaluation, and valid ground receiver metrics. For that reason the PMNet result
is kept as a transfer check in the appendix, not as a main method result.

\subsection{From image translation to receiver valid evaluation}

The first part of the project treated CKM prediction as aligned
image to image regression. This was useful for building the dataset pipeline:
the topology map, LoS/NLoS support, distance field and targets all live on the
same \(513\times513\) grid. The early cGAN and U-Net variants also exposed
practical issues such as checkerboard artifacts, excessive adversarial pressure
and the need for multiscale supervision.

The decisive correction was methodological rather than architectural. Once the
receiver mask was enforced, the task became prediction on ground receivers
only: buildings and padded pixels are not valid receiver locations. This is why
the final thesis does not mix early scores with the final leaderboard. They
remain useful as a development trace, but the valid comparison contract starts
once the ground mask is fixed.

\subsection{Decision record behind the final pipeline}

Table~\ref{tab:appendix_decision_record} summarizes the main decisions that
survived into the final method. The point is not that every intermediate idea
was successful, but that each retained constraint has a concrete failure mode
behind it.

\begin{table}[htbp]
\centering
\caption{Design decisions retained in the final pipeline.}
\label{tab:appendix_decision_record}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\textwidth}{p{0.24\textwidth}p{0.30\textwidth}X}
\toprule
Final decision & Evidence from attempts & Effect in the thesis \\
\midrule
City holdout split & Random or visually similar splits risked testing on morphology already seen during training. & Generalization is reported on unseen cities rather than on shuffled pixels or tiles. \\
Valid ground receiver mask & Including buildings inflated the apparent quality and mixed receiver and non receiver pixels. & Losses, RMSE and qualitative errors are computed over valid ground receivers. \\
Channel attenuation terminology & The HDF5 field is \texttt{path\_loss}, but the dense CKM target is an attenuation map. & Prose uses channel attenuation, while formulas, code names and cited path loss models keep the established term. \\
Frozen train only priors & Raw formulas were weak, but calibrated priors became strong anchors once fitted on training cities only. & Chapter~\ref{chap:methodology} separates path loss and spread prior fitting from residual learning. \\
LoS/NLoS and morphology regimes & Single global residuals hid hard blocked link behaviour and spread tails. & The priors and residual model expose visibility, topology and height regimes explicitly. \\
Distribution aware residual head & NLoS and spread histograms were not well represented by one symmetric Gaussian error. & The internal GMM head models residual structure, while final metrics use deterministic mixture means. \\
Final RMSE oriented fine tuning & Probabilistic losses were useful for diagnosis but not the best endpoint for the reported scalar RMSE. & The selected \textsc{HARP-Net CKM} stage optimizes the final deterministic dense maps. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Why the prior returned}

The first physical prior attempts were not successful as formulas alone. A raw
prior could be far worse than a neural model, especially in NLoS. However,
train only calibration made the prior informative enough to act as an anchor.
This distinction matters: the prior is not presented as a replacement for
learning, but as a structured baseline that gives the network the correct
coordinate system.

The negative evidence was equally important. PMNet style residual models,
topology experts and NLoS focused objectives improved parts of the map, but
the hard tails survived. The project therefore moved away from simply enlarging
the encoder and toward separating regimes, modelling distributions and freezing
strong calibrated estimators.

\subsection{Discarded routes that remain informative}

Several routes are deliberately not treated as final baselines. Conditional GAN
training produced sharper maps but put too much pressure on visual texture and
not enough on receiver valid physical error. Generic U-Net and PMNet style
backbones were useful architectural references, but they did not resolve the
CKM contract by themselves. Topology experts improved some regimes while adding
split and routing complexity. Knife edge and corridor channels were physically
reasonable diagnostics, yet the tested versions did not displace the calibrated
prior route. Distribution first models were essential because they exposed the
target statistics, but the final system returned to frozen priors because the
calibrated anchors were stronger and more auditable.

\subsection{Why the final priors + \textsc{HARP-Net CKM} are the endpoint}

The last attempts are the only ones that form the final methodology. The
path loss prior provides a height aware LoS branch and calibrated NLoS residual
structure. The delay spread and angular spread priors provide separate
LoS/NLoS, height and topology calibration. \textsc{HARP-Net CKM} then learns
bounded residual corrections over these frozen priors with a shared multi task
GMM head.

In other words, the final route is evolutionary but not arbitrary: direct
regression established feasibility, masked evaluation fixed the contract,
PMHHNet identified the remaining NLoS bottleneck, distribution first models
showed why tails mattered, and the frozen priors made the final residual model
small enough to improve rather than overwrite the physical estimator.

\subsection{What was moved out of the main chapters}

The distribution first PL and spread experiments remain part of the thesis
evidence, but their detailed per-expert tables are kept here conceptually
rather than in Chapter~\ref{chap:results}. The main chapters use them only for
the lesson they provide: direct neural maps can look plausible while averaging
away rare tails, and the final model should therefore keep calibrated priors
and a distribution aware residual head. This keeps the thesis shorter while
preserving the negative evidence that motivated the final design.

\subsection{Reproducibility anchors}

The concise appendix is tied to reproducible source artifacts rather than to
memory of the development process. The frozen path loss prior is documented in
the Try~78 formulas and scripts; the spread priors are documented in the Try~79
ridge calibration files; the final residual model and evaluation scripts are
kept in the final code repository cited in the bibliography; and the longer
attempt log remains in the source archive. The compiled thesis therefore keeps
the reasoning chain visible without turning the appendix into a full software
manual.

\endgroup

\chapter{Representative Final Model Panels}

The full generated panel set contains one candidate for each
\((\mathrm{open},\mathrm{mixed},\mathrm{dense})\times
(\mathrm{low},\mathrm{mid},\mathrm{high})\) audit group. The expanded reduced
thesis keeps three full-size qualitative panels: open/low, mixed/mid and
dense/mid. The remaining panels and their manifest are kept in
\texttt{img/thesis\_figures/try80\_appendix\_panels/}; the folder name keeps
the original run identifier, but the panels are final priors +
\textsc{HARP-Net CKM} outputs and can be regenerated with the panel-generation
script. The removed open/high Nazareth panel is not used because its
angular spread map is almost flat and the robust colour scale collapses to a
misleading near-zero range.

\begin{table}[htbp]
\centering
\caption{Representative final-model panel manifest.}
\label{tab:try80_appendix_panels}
\scriptsize
\begin{tabularx}{\textwidth}{llllX}
\toprule
Audit group & City & Sample & Status & Reason \\
\midrule
open/low & Segovia & sample\_13213 & PDF & Open low altitude case with visible geometry and residual correction. \\
mixed/mid & Nice & sample\_10828 & PDF & Mixed morphology and mid-height regime. \\
dense/mid & Vancouver & sample\_15265 & PDF & Dense held out test morphology; also used as the main paper/TFG qualitative panel. \\
\bottomrule
\end{tabularx}
\end{table}

Each panel has three columns (PL, DS and AS) and six rows: ground truth,
frozen prior, final prediction, support/context, prior error and model
correction. The last two rows are the diagnostic rows: a useful residual model
should correct the sign and location of the prior error.

\clearpage
\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.88\textheight,keepaspectratio]{img/thesis_figures/try80_appendix_panels/try80_panel_open_low_ant_Segovia_sample_13213_6x3.png}
\caption{\textsc{HARP-Net CKM} inspection panel for an open, low antenna
validation sample in Segovia.}
\label{fig:appendix_try80_open_low}
\end{figure}

\clearpage
\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.88\textheight,keepaspectratio]{img/thesis_figures/try80_appendix_panels/try80_panel_mixed_mid_ant_Nice_sample_10828_6x3.png}
\caption{\textsc{HARP-Net CKM} inspection panel for a mixed, mid antenna
validation sample in Nice.}
\label{fig:appendix_try80_mixed_mid}
\end{figure}

\clearpage
\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.88\textheight,keepaspectratio]{img/thesis_figures/try80_appendix_panels/try80_panel_dense_mid_ant_Vancouver_sample_15265_6x3.png}
\caption{\textsc{HARP-Net CKM} inspection panel for a dense, mid antenna
held out test sample in Vancouver.}
\label{fig:try80_panel}
\end{figure}

\chapter{Additional Results and Qualitative Diagnostics}
\label{app:qualitative_diagnostics}

\begingroup
\small
\setlength{\parskip}{2pt plus 0.5pt minus 0.3pt}
\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}
\captionsetup{font=scriptsize,skip=2pt}

This one page appendix keeps only the two diagnostics that are useful to audit
the final interpretation. Full split bars, all city subexpert tables, the full
qualitative gallery and long derivations remain in the source archive and
generated image folders.

\begin{figure}[H]
\centering
\begin{subfigure}{\textwidth}
\centering
\includegraphics[width=\textwidth,height=0.25\textheight,keepaspectratio]{img/thesis_figures/results_addendum/try76_los_vs_nlos_distribution.png}
\caption{Distribution first GMM diagnostic for PL. Thick curves show summed
LoS/NLoS GMM densities; faint dotted curves show the individual Gaussian
components.}
\label{fig:try76_los_vs_nlos_distribution}
\end{subfigure}

\vspace{1pt}
\begin{subfigure}{\textwidth}
\centering
\includegraphics[width=\textwidth,height=0.19\textheight,keepaspectratio]{img/thesis_figures/results_addendum/try79_quantized_spread_example.png}
\caption{Spread target quantisation: predictions are continuous while the
released targets contain staircase like and sparse integer valued structures.}
\label{fig:try79_quantized_spread_example}
\end{subfigure}
\caption{Compact additional diagnostics. The GMM plot exposed different
LoS/NLoS marginals and motivated distribution aware heads; the quantisation
panel records the target storage limitation discussed in Chapter~\ref{chap:results}.}
\end{figure}

\noindent\textbf{Additional source material.}
The nine panel gallery, row level 80 attempt log, all city subexpert tables and
split bars are omitted from this compact appendix but remain in
\texttt{appendices.tex} and the generated figure folders. The final prior and
\textsc{HARP-Net CKM} mathematical derivations are included in
Appendix~\ref{app:method_details} through \texttt{prior\_detail\_try78.tex},
\texttt{prior\_detail\_try79.tex} and \texttt{prior\_detail\_try80.tex}. The
last filename keeps the original run identifier, but it documents the
\textsc{HARP-Net CKM} residual model; all three files also remain as separate
source files for traceability.
\endgroup

\chapter{Detailed Method Formulas}
\label{app:method_details}
\begingroup
\small

This appendix keeps the formula level details that support
Chapter~\ref{chap:methodology}. It is intentionally placed outside the main
methodology chapter so that the thesis first presents the final system clearly,
while still preserving the exact channel attenuation prior, spread prior and
\textsc{HARP-Net CKM} residual head definitions used for reproduction.

\section{Channel Attenuation Prior Details}
\input{prior_detail_try78}

\section{Delay and Angular Spread Prior Details}
\input{prior_detail_try79}

\section{\textsc{HARP-Net CKM} Residual Model Details}
\input{prior_detail_try80}

\endgroup

\chapter{Declaration on Generative AI Use}
\label{app:generative_ai_declaration}

Generative AI tools were used during the preparation and software development
of this thesis. The tools used were OpenAI Codex, GPT 5.5, and GPT 5.5 Pro.
They were used as assistant tools for writing support, literature and
terminology checks, LaTeX editing, consistency audits, Python code generation,
debugging, and refactoring.

In the software work, these tools were useful for translating author defined
ideas into executable code. For example, they helped convert prototype logic
into scripts, port calibrated priors from NumPy prototypes to PyTorch
implementations, wire evaluation utilities, inspect errors, and keep thesis and
paper sources consistent. They also supported drafting and revising text, but
all references and claims were checked by the author.

The generative AI tools did not originate the scientific direction, research
questions, model ideas, experiment choices, final interpretation, reported
numbers, plots, or conclusions of the project. The project design, validation
protocol, final source selection and responsibility for the submitted document
remain with the author. Numerical results were obtained from the project
scripts, stored artifacts and compiled thesis sources, not from generative model
outputs.

The CKM dataset itself was processed through local scripts and stored experiment
outputs rather than pasted into prompts. Code changes and final text were
reviewed by the author before inclusion.
